function [ dev ] = sATI_vhintan(name, exp )
%SATI_VHINTAN Summary of this function goes here
%   this method would have to query the experiment to identify the files to read

%% DEV = SATI_VHINTAN(NAME,EXP)

%%refer to maybe already existing functions or information to query the
%%exp and name to obtain the path for dev files 


end

