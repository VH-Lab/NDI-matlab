{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/VH-Lab/NDI-matlab/blob/main/ndi_common/schema_documents/daq/daqmetadatareader_schema.json",
  "title": "DAQ Metadata Reader",
  "description": "Schema for data acquisition metadata reader",
  "type": "object",
  "properties": {
    "document_class": {
      "type": "object",
      "description": "Document class definitions and properties",
      "required": ["class_name", "class_version", "superclasses"],
      "properties": {
        "class_name": {
          "type": "string",
          "description": "Name of the document class",
          "pattern": "^[a-z][a-z0-9_]*$"
        },
        "class_version": {
          "type": "integer",
          "description": "Version number of the document class",
          "minimum": 1
        },
        "superclasses": {
          "type": "array",
          "description": "Parent classes from which this class inherits properties and behavior",
          "items": {
            "type": "object",
            "required": ["definition"],
            "properties": {
              "definition": {
                "type": "string",
                "description": "Path to the superclass document definition"
              }
            }
          }
        }
      }
    },
    "depends_on": {
      "type": "array",
      "description": "Document dependencies with validation rules",
      "items": {
        "type": "object",
        "required": ["name", "value"],
        "properties": {
          "name": {
            "type": "string",
            "description": "Name of the dependency"
          },
          "value": {
            "type": "string",
            "description": "Value of the dependency"
          }
        }
      }
    },
    "files": {
      "type": "object",
      "description": "Files associated with this document",
      "required": ["file_list"],
      "properties": {
        "file_list": {
          "type": "array",
          "description": "List of associated files",
          "items": {
            "type": "string"
          }
        }
      }
    },
    "daqmetadatareader": {
      "type": "object",
      "description": "DAQ metadata reader properties",
      "required": ["ndi_daqmetadatareader_class", "tab_separated_file_parameter"],
      "properties": {
        "ndi_daqmetadatareader_class": {
          "type": "string",
          "description": "The ndi class name of the ndi.daq.metadatareader being stored.",
          "default": "",
          "queryable": true
        },
        "tab_separated_file_parameter": {
          "type": "string",
          "description": "A tab-separated file parameter for identifying the file in the epoch to be read",
          "default": "",
          "queryable": true
        }
      }
    }
  },
  "allOf": [
    {
      "description": "Inherits all properties from base schema",
      "$ref": "$NDISCHEMAPATH/base_schema.json"
    }
  ],
  "required": ["document_class", "depends_on", "files", "daqmetadatareader"],
  "additionalProperties": false
}
