function type = getclocktype(NSD_clock)
% GETCLOCKTYPE - get the clock type for specific device
%
%   TYPE = GETCLOCKTYPE(NSD_CLOCK)  returns a clock type that is specified for certain
%   device

type = NSD_clock.type;

