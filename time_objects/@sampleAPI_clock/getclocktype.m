function type = getclocktype(sampleAPI_clock)
% GETCLOCKTYPE - get the clock type for specific device
%
%   TYPE = GETCLOCKTYPE(SAMPLEAPI_CLOCK)  returns a clock type that is specified for certain
%   device

type = sampleAPI_clock.type;

