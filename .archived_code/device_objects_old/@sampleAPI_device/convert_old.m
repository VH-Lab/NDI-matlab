function [c] = convert(d1,time,desired_clock,original_clock)

% CONVERT - convert a explicit time from the device clock to a desired
% clock

% [C] = CONVERT(D1,TIME,DESIRED_CLOCK,ORIGINAL_CLOCK)

...
    
end 
