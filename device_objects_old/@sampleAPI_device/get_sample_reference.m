function [ reference ] = get_sample_reference(NSD)
%GET_SAMPLE_REFERENCE get the type of time reference for each sample
%   reference can be in units of "time" of "samples"
reference = NSD.reference;

end

